framework:
  secret: OmgItIsASecretOfThieBundleTest
  test:   ~

velikonja_labby:
  remote:
    hostname: localhost
    path:     /tmp/labby-bundle-tests
  fs:
    maps:
      uploads:
        src: localhost:/tmp/labby-bundle-tests
        dst: web/codeCoverage2/
doctrine:
  dbal:
    driver:   pdo_mysql
    host:     localhost
    port:     ~
    dbname:   test
    user:     root
    password: ~
    charset:  UTF8

#imports:
#    - { resource: ../../../../Resources/config/services.yml }
